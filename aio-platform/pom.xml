<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <artifactId>aio-platform</artifactId>
    <name>Alfresco Platform/Repository JAR Module</name>
    <description>Platform/Repo JAR Module (to be included in the alfresco.war) - part of AIO - SDK 4.11.0</description>
    <packaging>jar</packaging>

    <parent>
        <groupId>ae.ac.cud</groupId>
        <artifactId>aio</artifactId>
        <version>1.0-SNAPSHOT</version>
    </parent>

    <properties>
    </properties>

    <dependencies>
    </dependencies>

    <build>
        <!--
            Override parent resource filtering: .xlsx files are binary
            (ZIP format) and must NOT be filtered, otherwise Apache POI
            will reject them as "unsupported file type: UNKNOWN".
            The parent POM sets <filtering>true</filtering> by default,
            which corrupts binary resources.

            combine.self="override" is REQUIRED because Maven merges
            child <resources> with parent <resources> by default.
            Without this, the parent's unfiltered resource block
            (filtering=true, no excludes) still applies.
        -->
        <resources combine.self="override">
            <resource>
                <directory>src/main/resources</directory>
                <filtering>true</filtering>
                <excludes>
                    <exclude>**/*.xlsx</exclude>
                </excludes>
            </resource>
            <resource>
                <directory>src/main/resources</directory>
                <filtering>false</filtering>
                <includes>
                    <include>**/*.xlsx</include>
                </includes>
            </resource>
        </resources>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-assembly-plugin</artifactId>
            </plugin>
        </plugins>
    </build>

</project>