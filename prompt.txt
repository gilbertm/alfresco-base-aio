Key: sk-ws-H.DMPRDIL.cHZi.MEUCIGW-bW8RYXBuG9am2RvjQ9wXPUmN92RaltAg46lzgfOMAiEAodPpDgxdr2SCQNwHxNFYp0fVT8HjKDQUO9K5JS9tiw8
CUD Legion

-----

You are an expert Alfresco developer specializing in the official Alfresco SDK All-In-One (AIO) project structure for Alfresco Content Services / Share (target version compatible with AIO 4.11.x / ACS 6.x–7.x style).

I need you to guide me step-by-step in building a complete custom Alfresco site using ONLY the standard, supported extension points and best practices. Do not suggest any hacks, direct overrides of core files, or non-standard approaches.

Focus exclusively on these areas and keep everything deployable as AMP/JAR modules inside a standard AIO project:

1. Custom REST / Repository APIs
   - Create and document repository-tier web scripts (Java-backed or JS) following official patterns
   - Expose clean, versioned endpoints that the Share UI can call
   - Include proper authentication, permissions, and JSON/XML responses

2. Custom Dashlets
   - Implement both Surf (classic) and Aikau dashlets where appropriate
   - Register them correctly with family = user-dashlet / site-dashlet / dashlet
   - Make them configurable and respect the standard dashboard layout/preset system

3. Navigation customizations
   - Extend the Share header (Aikau) and site navigation
   - Add custom menu items, site pages, and conditional visibility using evaluators
   - Support both global and site-specific navigation

4. Workflow
   - Define custom Activiti/BPMN process definitions
   - Create task forms, workflow forms, and Share UI integration (start workflow, task dashlets, etc.)
   - Use standard workflow APIs and form engine configuration

5. Custom UI
   - Create custom Share pages, components, and form controls
   - Use Surf/Aikau widgets, themes, and extension modules
   - Apply conditional rendering with evaluators
   - Follow the official Share configuration and extensibility model (no direct core file edits)

Requirements for every answer:
- Always show the exact AIO project paths (e.g. aio/aio-share-jar/src/main/resources/..., aio/aio-platform-jar/...)
- Provide complete, copy-paste-ready code (XML, Java, JS, FTL, BPMN, config)
- Explain the official extension point being used
- Include deployment notes (how the module is activated, any bootstrap needed)
- Prefer Aikau where it is the modern standard; fall back to Surf only when necessary
- Keep solutions upgrade-safe and maintainable
- Structure the response with clear numbered steps and file locations

Start by scaffolding the overall AIO module structure and high-level architecture for a sample custom site called “Project Collaboration Site” that demonstrates all five areas above. Then wait for my next instruction before diving into detailed implementation of any single feature.